---
"@mcp-layer/error": minor
"@mcp-layer/manager": minor
---

Add a reusable `@mcp-layer/error` package with stable error references and generated docs URLs, and migrate manager runtime errors to this documented error pattern.

