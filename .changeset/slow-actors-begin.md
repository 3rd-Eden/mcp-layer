---
"@mcp-layer/connect": minor
"@mcp-layer/cli": minor
"@mcp-layer/test-server": patch
---

Add connection timeouts to avoid hanging CLI sessions and update the test server to use stateful Streamable HTTP sessions.
