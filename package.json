{
  "name": "mcp-layer-workspace",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "type": "module",
  "scripts": {
    "build:types": "pnpm -r --if-present build:types",
    "test": "pnpm build:types && node --test --test-timeout=30000 test/**/*.test.js packages/*/test/*.test.js packages/*/test/**/*.test.js",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "@types/node": "^25.3.3",
    "typescript": "^5.9.3"
  }
}
